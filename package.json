{
  "name": "prendus-learning-platform",
  "version": "0.3.0",
  "description": "The tool to create and upload videos and quizzes",
  "scripts": {
    "start": "zwitterion --serve-dir app/client",
    "production-build": "zwitterion --serve-dir app/client --build --output-dir dist",
    "test": "electron node_modules/scram-engine/main.js --entry-file test/client/index.html --window"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:bmackley/prendus-learning-platform.git"
  },
  "homepage": "https://github.com/bmackley/prendus-learning-platform",
  "dependencies": {},
  "devDependencies": {
    "@types/polymer": "^1.1.29",
    "scram-engine": "^0.4.5",
    "zwitterion": "^0.7.0"
  }
}
