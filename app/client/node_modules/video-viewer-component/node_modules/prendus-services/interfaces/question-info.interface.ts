import {InputAnswer} from './input-answer.interface.ts';
import {CheckOrRadioAnswer} from './check-or-radio-answer.interface.ts';

export interface QuestionInfo {
    uid: string,
    transformedText: string,
    text: string,
    code: string,
    answer: string | InputAnswer | CheckOrRadioAnswer,
    hint: string,
    private: boolean,
    showConfidenceLevel: boolean,
    questionSessionId: string,
    previewQuestionId: string,
    userInputs: string[],
    userCheckboxes: string[],
    userRadios: string[]
}
