//TODO this interface will describe model modules and the basic functions that they should all have available

export interface PrendusModel {
    createOrUpdate: (id: string, data: PrendusEntity) => Promise<string>;
    getById: (id: string, ) => Promise<PrendusEntity>;
}
