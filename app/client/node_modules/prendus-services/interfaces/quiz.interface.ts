import {QuestionSettings} from './question-settings.interface.ts';

export interface Quiz {
    id: string,
    uid: string,
    private: boolean,
    questions: {
        [questionId: string]: {
            settings: QuestionSettings
        }
    }
}
