import {Answer} from './answer.type.ts';
import {UserAnswerInfo} from './user-answer-info.interface.ts';

export interface QuestionGrades {
    [questionId: string]: {
        correct: boolean,
        userAnswerInfo: UserAnswerInfo,
        answer: Answer,
        questionSessionId: string
    }
}
