import {Answer} from '../../../node_modules/prendus-services/interfaces/answer.type.ts';
import {UserAnswerInfo} from '../../../node_modules/prendus-services/interfaces/user-answer-info.interface.ts';

export interface QuestionGrades {
    [questionId: string]: {
        correct: boolean,
        userAnswerInfo: UserAnswerInfo,
        answer: Answer,
        questionSessionId: string
    }
}
