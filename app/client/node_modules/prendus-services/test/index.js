describe('running tests', function() {
    this.timeout(30000);

    it('should run all the tests', function (done) {
        System.import('firebase.service.test.ts').then(() => {
            System.import('problem.model.test.ts').then(() => {
                done();
            });
        });
    });
});
